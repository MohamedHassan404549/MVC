@model Dept_Loc

<h2>Edit Project</h2>

<form asp-action="SaveUpdate">
    <input type="hidden" asp-for="Dnumber" />
     <div class="form-group">
        <label for="exampleInputEmail1">Dnumber</label>
        <input type="text" value="@Model?.Dnumber" class="form-control" name="Dnumber" />
    </div>
     <div class="form-group">
        <label for="exampleInputEmail1">Dept_Location</label>
        <input type="text" value="@Model?.Location" class="form-control" name="Location" />
    </div>
    <div class="form-group"/>
        <label for="exampleInputEmail1">Department</label>
    <input type="text" value="@Model?.Dnumber" class="form-control" name="Dnumber" />
    

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>